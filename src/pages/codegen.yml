overwrite: true
schema: "http://localhost:3001/api/graphql"
documents: "./src/pages/*.{ts,tsx}"
generates:
  src/pages/generated.apollo.types.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-react-apollo"
    config:
      withHooks: true
